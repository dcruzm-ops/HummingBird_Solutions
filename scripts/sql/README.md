# Legacy SQL path (compatibilidad)

Estos archivos se mantienen para compatibilidad con flujos que aún referencian `scripts/sql`.

Fuente oficial y orden de ejecución: `BaseDatos/00_ORDEN_EJECUCION.md`.
